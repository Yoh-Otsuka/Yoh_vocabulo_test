class CsvData {
  // フィールドを生成
  double top_x;
  double top_y;
  double width;
  double height;
  int level;
  bool show;

  // コンストラクタ
  CsvData({this.top_x, this.top_y, this.width, this.height, this.level, this.show});
}