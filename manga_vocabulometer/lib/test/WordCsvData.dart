class WordCsvData {
  // フィールドを生成
  double top_x;
  double top_y;
  double width;
  double height;
  int level;
  String jp;
  String eng;
  bool registered;


  // コンストラクタ
  WordCsvData({this.top_x, this.top_y, this.width, this.height, this.level, this.jp, this.eng, this.registered});
}